#include "monty.h"

/**
* perform_nop - function does nothing
* @head: double head pointer to the stack
* @count: line count
*
* Return: nothing
*/
void perform_nop(stack_t **head, unsigned int count)
{
        (void) count;
        (void) head;
}
